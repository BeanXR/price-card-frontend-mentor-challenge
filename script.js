const button = document.querySelector('button')

button.addEventListener('click',arg => alert('You have clicked the sign up button'))
// of just add the function name without invoke/()
// if adding onclick attribute to html element, need the invoke/()/functionName()